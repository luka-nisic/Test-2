name: workflow_dispatch
#on: [repository_dispatch]
on: [workflow_dispatch]
jobs:
  cypress-run:
    runs-on: ubuntu-latest
    steps:
      - name: Testing inputs
        #if: ${{ github.ref == 're' }}
      - run: echo "Triggering Wokflow Dispatch but from test branch"
      - run: echo ${{ github.ref }}
      - name: Checkout
        uses: actions/checkout@v2
